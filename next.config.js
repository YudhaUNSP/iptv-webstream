/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com', 'i.imgur.com', 'iptv-org.github.io'],
  },
};

module.exports = nextConfig;